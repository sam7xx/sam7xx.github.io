# Stellar 主题升级迁移记录（1.33.1 → 1.44.0）

> 用途：记录本博客从 Stellar 1.33.1 升级到 1.44.0 的全部定制迁移方法、踩坑记录与验证清单，供下次升级主题时照做。
> 最后更新：2026-08-26

## 一、环境基线

| 项目 | 版本 | 说明 |
|---|---|---|
| Node.js | **v24.19.0**（nvm `lts/krypton`） | ⚠️ 必须 ≤24：Node 25/26 移除了 `SlowBuffer`，老 JWT 依赖链会崩 |
| hexo | 8.1.1（博客本地） | `hexo version` 确认走本地核心 |
| npm | 11.17.0 | npm 12 会默认拦截依赖 install 脚本 |
| 主题 | stellar 1.44.0（`themes/stellar` 目录） | 主题目录优先于 node_modules 里的 npm 包 |

## 二、升级步骤（下次照做）

```bash
cd /media/sam/xx/blog

# 1. 备份当前主题（含所有定制）
cp -r themes/stellar themes/stellar.bak-1.33.1

# 2. 安装新版到 node_modules（仅作源码来源，hexo 不会直接用它）
npm i hexo-theme-stellar@latest

# 3. 整体替换主题目录（保留你自己的 _config.yml）
cp themes/stellar/_config.yml /tmp/stellar-config-old.yml
rm -rf themes/stellar && mkdir -p themes/stellar
cp -a node_modules/hexo-theme-stellar/. themes/stellar/
cp /tmp/stellar-config-old.yml themes/stellar/_config.yml

# 4. 对比新旧配置差异（图标前缀、CDN 等可能变化）
diff /tmp/stellar-config-old.yml node_modules/hexo-theme-stellar/_config.yml

# 5. 重建验证（⚠️ 必须 clean，见踩坑 #4）
hexo clean && hexo generate
```

**校验**：`hexo version` 应显示本地核心版本；页面横幅应显示新版本号；浏览器强刷一次（CSS 版本戳已动态化，见踩坑 #5）。

## 三、定制功能迁移清单

> 判定方法：`diff -rq` 对比备份目录与 npm 包同版本原版，甄别真实定制（minifier 压缩导致的差异用"去空白后对比"过滤）。

### 3.1 直接复制（备份 → 新主题）

| 文件 | 说明 |
|---|---|
| `layout/_partial/main/footer.ejs` | 页脚统计（博客/笔记/文档/字数），依赖 hexo-wordcount |
| `layout/_partial/sidebar/menu.ejs` | "更多"下拉菜单（含动态图标渲染逻辑） |
| `source/css/_plugins/aplayer.styl` | 播放器配色（浅/深主题变量 `--aplayer-*`） |
| `source/css/_plugins/comments/waline.styl` | 评论框颜色 → 新版移动到 `source/css/comments/waline.styl` |
| `source/css/art-title.css`、`source/js/darkmode.js`、`vercel-analytics.js`、`sakura.js` | 自加文件（sakura 在配置中注释禁用） |
| `giscus.json`、`_data/widgets.yml` | 数据/配置 |

### 3.2 合并迁移（备份 → 1.44.0 结构）

| 定制 | 迁移方式 |
|---|---|
| 面包屑字数/阅读时长/日期 | **新版原生实现**：仅需在 `_config.stellar.yml` 加 `article.reading_time: true` |
| 站长验证 meta（Bing/百度） | `layout/_partial/head.ejs` 手工合入 |
| 深色模式图片滤镜 + 背景遮罩 | 追加到 `source/css/_defines/theme_base.styl`（含 `$site-background-opacity` 兜底、`html::before/::after` 壁纸、img 滤镜规则） |
| 布局变量（主区 999px/侧栏 255px/间距 15px） | 改 `_custom.styl` 的 `:root`（`--width-main`、`--side-content-width`、`--gap-base/page`） |
| 导航栏模糊修复 | 1.44.0 原生已用卡片背景替代 |
| `pretty_url.js`/`chat.js` 容错修复 | **1.44.0 原生已包含**（上游采纳），无需迁移 |
| 站点背景图 | 原生走 `style.site.background-image` 配置（CYFChk） |

### 3.3 音乐播放器 + pjax（本博客特有注入，位于 `_config.yml` 的 `inject:`）

- `inject.head`：APlayer.min.css
- `inject.script`（body 尾部，顺序固定）：
  1. APlayer.min.js
  2. **幂等创建脚本**：`if(!document.querySelector('meting-js,#global-player'))` 时，把 meting-js 元素包进 `<div id="global-player">` 再 append——**必须用包裹 div**（meting 用元素 `id` 属性当歌单 ID，不能直接用作标记）
  3. Meting.min.js
  4. 悬浮按钮 `<div id="player-fab">`
  5. 交互脚本：fab 切换 `#global-player` 的 `player-open`/`aplayer-withlist`；**主题切换事件委托**（pjax 换页后按钮元素被替换，直接绑定失效）
  6. **pjax 脚本**：拦截同源链接点击 → fetch 替换 `#start` 内容 → 重执行新页内联脚本（src 去重）→ pushState；popstate 同理

- 播放器样式在 `_custom.styl`：`#global-player`（fixed 定位、340px、默认 `display:none`、`player-open` 时显示、背景 `var(--aplayer-bg)` 跟随主题）

## 四、踩坑记录（重点，下次必看）

### 4.1 Node 版本：SlowBuffer 被移除（重大）
`buffer-equal-constant-time@1.0.1` 引用 `SlowBuffer`，**Node 22 前的文档说法有误：实测 v24 仍有、v25/v26 移除**。npm 官方文档核对：`https://nodejs.org/docs/latest-v24.x/api/buffer.html`。
- 症状：`Cannot read properties of undefined (reading 'prototype')`，来自 `jsonwebtoken→jws→jwa→buffer-equal-constant-time`
- 修复：`nvm install 24 && nvm alias default lts/krypton`（⚠️ 不要 `nvm alias default 24`，裸版本号无法解析，新 shell 会没有 node）

### 4.2 npm 12 拦截 install 脚本
依赖的 `postinstall`（hexo-util 高亮构建、图片优化二进制等）默认不执行。
- hexo-util 的 postinstall 构建 highlight.js，被拦会导致代码块高亮失效
- 处理：`npm install-scripts approve hexo-util`（写入 package.json 的 `allowScripts`）
- 全局安装用 `npm install -g --allow-scripts=hexo-util`；`.npmrc` 的 allow-scripts 会被 package.json 的 allowScripts 字段忽略（正常，别删）

### 4.3 主题目录 vs npm 包（关键认知）
hexo 优先加载 `themes/<name>` 目录；`node_modules` 里的 npm 包**不会被使用**。
- `npm i hexo-theme-stellar@latest` 只是拿到新源码，必须手动替换 `themes/stellar` 目录
- 升级提示"本地版本 1.33.1"指的是 `themes/stellar/package.json`

### 4.4 hexo 增量构建不追踪 stylus @import（重大）
改了 `_custom.styl` 等被 @import 的文件后，直接 `hexo generate` **不会重渲染 main.css**（mtime 不更新）。**改任何 CSS 必须 `hexo clean && hexo generate`**，否则改动不进产物。

### 4.5 CSS 缓存戳必须动态化
主题用 `main.css?v=${stellar_info('version')}`，版本号不变 → 浏览器永远用旧 CSS，改了什么"都没生效"。
- 已改为 `?v=${stellar_info('version')}-${Date.now()}`（`themes/stellar/layout/_partial/head.ejs`），每次构建自动换 URL

### 4.6 hexo server 陈旧内存状态回写 public（重大）
运行中的 `hexo server` 用**启动时的内存状态**渲染；任何文件变化触发它重渲染时，会用旧状态**覆盖 public**——外部改的构建会被悄悄还原。
- 症状：改了没效果、构建产物被回退
- 处理：改完必须**重启 hexo server**（Ctrl+C → `hexo s`）

### 4.7 APlayer `fixed` 模式强制 narrow（播放器定制最大坑）
`fixed="true"` 会让 APlayer 自动进入 `aplayer-narrow` 迷你模式：播放条 84px 宽、歌单 `max-height:0` 被隐藏；narrow 规则 `width:66px!important` 后加载压过一切覆盖。
- **解决方案：不用 fixed 模式**——普通 APlayer 包在自定位容器 `#global-player` 里，左对齐/歌单展示天然成立

### 4.8 HTML 压缩器折叠布尔属性
`autoplay="false"` 会被 html-minifier 折叠成 `autoplay`（变自动播放）。播放器必须用 **JS `setAttribute` 构造**，不要写裸 HTML 属性。

### 4.9 clean-css 合并同选择器规则丢属性
同一个选择器多条规则（如 `.aplayer-list` 的定位/背景/边框）被 clean-css 合并时**可能丢弃属性**。对策：
- 用不同选择器串（如 `ol.aplayer-list`）规避合并
- 改完**务必 grep 编译产物确认属性存活**（`public/css/main.css`）

### 4.10 主题的 inject 键名
Stellar 的注入是 `inject.head` 和 `inject.script`（scripts.ejs 在 `</body>` 前渲染 script 项），**不是** hexo 原生的 `bodyEnd`。

### 4.11 其他
- stylus 里 `if !$var` 守卫对未定义变量不生效（按字面量真值处理）→ 兜底值用无条件赋值
- stylus 不插值 `calc()` 内变量 → 深色蒙版 `0.1 + 0.7` 直接写加法
- hexo-all-minifier 会**原地压缩主题源文件**（css/js 变单行）→ node_modules 里的包是唯一未压缩副本，别删
- pjax 重执行页面脚本 → 所有注入脚本必须幂等（`window.__xxx` 标志/元素存在性守卫）

## 五、升级后验证清单

- [ ] `hexo clean && hexo generate` 0 ERROR
- [ ] 页面横幅显示新版本号（`Welcome to Stellar x.y.z`）
- [ ] 文章页：字数 + 阅读时长 + 发布/更新时间
- [ ] 页脚统计：已发布博客 N 篇 · 笔记 · 文档 · 总字数
- [ ] "更多"下拉：图标显示、向下生长、全部可点
- [ ] 深浅色切换：按钮可用、主题跟随、切换平滑
- [ ] 音乐播放器：圆圈 → 展开 340px 卡片（播放条+歌单左对齐一体）→ 收起无白条；播放中切页不断播（pjax）
- [ ] 评论区（waline）颜色正常
- [ ] 图片：深色模式滤镜、fancybox
- [ ] 站点背景图正常、移动端无全宽白条
- [ ] 浏览器强刷（CSS URL 已动态化，正常刷新即可）

## 六、关键命令速查

```bash
hexo clean && hexo generate        # 改 CSS/主题后必须 clean（见 4.4）
nvm use default                    # Node 24（lts/krypton）
npm i hexo-theme-stellar@latest    # 拿新源码（需手动替换 themes/stellar）
# 定制文件都在：
#   themes/stellar/ + _config.yml(inject) + _config.stellar.yml + source/_posts/
```

## 七、备份位置

- 旧主题完整备份：`themes/stellar.bak-1.33.1`（确认新主题稳定后可删除）
- 每次升级前先做同样备份：`cp -r themes/stellar themes/stellar.bak-<旧版本>`

## 八、本次升级（1.33.1 → 1.44.0）新增功能与优化

### 主题原生新增
- 面包屑原生字数+阅读时长（`article.reading_time: true` 即开即用）
- 原生日期组件 dateinfo（发布/更新时间，wiki/notebook/post 分场景）
- AI 成分标签 `ai_label`
- 横幅升级：图片遮罩 banner-mask、背景图文字自适应 data-text-adaptive
- 通用主题色渐变体系（x-theme-gradient / x-theme-light / --item-theme）
- 导航栏吸顶玻璃效果（pinned + bar-glass），替代手改的"取消模糊"
- 站点背景原生化（style.site.background-image + blur 配置）
- 三态主题切换（light → dark → auto）
- 菜单重构为 collection 组件渲染
- 插件注册体系 stellar.initPlugin（队列+补注册）
- 新内容组件：mbti / okr / read / pin-slider / collection-preview / wiki-card 等
- 字号系统 calc 相对化（--fs-content-base）
- 吸收社区修复：pretty_url 绝对 URL、chat.js 容错

### 本次迁移新做
- pjax 无刷新导航（音乐播放不断播）
- 音乐播放器（右下角悬浮按钮 + 340px 卡片，默认收起）
- 主题切换事件委托（pjax 兼容）
- 定制功能全量迁移适配（页脚统计/下拉菜单/播放器/蒙版/滤镜/布局）

### 工程优化
- CSS 缓存戳动态化（每次构建换 URL，杜绝缓存问题）
- 构建流程规范（改 CSS 必须 clean；改完重启 server）
- 注入脚本幂等化（pjax 安全）
